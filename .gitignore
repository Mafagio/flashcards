.DS_Store

# Backend Battle — ne jamais committer secrets / base / caches
backend/.env
*.db
*.db-wal
*.db-shm
__pycache__/
*.pyc

# Payloads d'examens (énoncés + corrigés transcrits VERBATIM) — BACKEND UNIQUEMENT.
# Vit sur le volume Railway, jamais dans le repo public.
backend/ts_exams*.json
