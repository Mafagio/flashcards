# Copie en .env et adapte.
DB_PATH=/data/battle.db
# Origine de ton site GitHub Pages (pour CORS). En prod, NE PAS laisser *.
ALLOWED_ORIGINS=https://mafagio.github.io
# Correction LLM. Sans clé -> correcteur "stub" hors-ligne (pour tester).
ANTHROPIC_API_KEY=
GRADER_MODEL=claude-sonnet-4-5
